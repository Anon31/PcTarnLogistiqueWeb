import { EnumsDynamicPipe } from './enums-dynamic-pipe';

describe('EnumsDynamicPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumsDynamicPipe();
    expect(pipe).toBeTruthy();
  });
});
