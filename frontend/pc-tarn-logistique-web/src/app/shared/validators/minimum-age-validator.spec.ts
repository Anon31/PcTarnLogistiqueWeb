import { MinimumAgeValidator } from './minimum-age-validator';

describe('MinimumAgeValidator', () => {
  it('should create an instance', () => {
    expect(new MinimumAgeValidator()).toBeTruthy();
  });
});
