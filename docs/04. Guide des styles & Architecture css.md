## 🎨 Guide des Styles & Architecture CSS

Ce document explique l'organisation des styles pour le projet Frontend PC Tarn Logistique. Nous utilisons une approche hybride combinant l'efficacité de Tailwind, la richesse de PrimeNG et la précision de nos tokens CSS personnalisés.

# 📂 Architecture des Dossiers

L'entrée principale des styles est le fichier src/styles.css. Il orchestre le chargement des librairies et de nos styles personnalisés.

# 🚦 Ordre de Priorité & Responsabilités

Pour garder le code propre et maintenable, suivez cet ordre de décision pour styliser un élément :

### 🥇 1. Tailwind CSS (Priorité Absolue pour le Layout)

Utilisez Tailwind pour 90% de votre CSS.

Pourquoi ? C'est rapide, standard et gère le responsive nativement.

Usage : Marges, Paddings, Flexbox, Grid, Largeurs, Responsive, Typographie simple.

Exemple :

<!-- ✅ BON : Layout et espacement via Tailwind -->
<div class="flex flex-col md:flex-row gap-4 p-4 bg-white rounded-lg">

### 🥈 2. PrimeNG (Pour les Composants UI)

Utilisez PrimeNG pour les éléments interactifs complexes.

Pourquoi ? Accessibilité et cohérence visuelle garanties.

Usage : Boutons, Tableaux, Dialogues, Inputs, Datepickers.

Personnalisation : Via le plugin tailwindcss-primeui ou les classes styleClass.

Exemple :

<!-- ✅ BON : Composant riche -->

`<p-button label="Valider" icon="pi pi-check" severity="primary"></p-button>
`

### 🥉 3. Custom CSS (Dernier recours & Thème)

Utilisez nos fichiers CSS (\_tokens.css, \_base.css) pour l'identité visuelle.

Pourquoi ? Pour définir la charte graphique (Couleurs Protection Civile) et les règles globales.

Usage : Définition des variables --primary-500, styles globaux du body, classes complexes réutilisables introuvables dans Tailwind.

# 📘 Détail des Fichiers Custom

Voici à quoi servent vos fichiers dans styles/css/ :

\_tokens.css (La Banque de Données)

C'est la Source de Vérité. Si on change le bleu ici, il change partout.

Contient les variables :root { ... }.

Définit les couleurs --primary-500 (Bleu PC) et --secondary-500 (Orange PC).

Utilisé par Tailwind (via le plugin) et PrimeNG (via le preset).

\_base.css (Les Fondations)

Styles par défaut pour les balises HTML nues (html, body, h1, a).

Configuration de la police d'écriture globale.

Note : Ne mettez pas de classes utilitaires ici.

\_container.css (Layouts Spécifiques)

Sert à créer des classes de conteneurs réutilisables spécifiques au projet si Tailwind ne suffit pas.

Exemple : Une classe .layout-wrapper qui gère un comportement de grille très spécifique à l'application.

# 💡 Bonnes Pratiques

Le Responsive Design

Ne faites jamais de Media Queries (@media) dans les fichiers CSS si Tailwind peut le faire.

Mobile First : Les classes de base sont pour mobile.

Breakpoints : Utilisez md: (Tablette), lg: (Desktop).

<!-- Rouge sur mobile, Bleu sur écran moyen, Vert sur grand écran -->
<div class="bg-red-500 md:bg-blue-500 lg:bg-green-500"></div>

Les Couleurs

N'utilisez jamais de code hexadécimal (#004080) directement dans le HTML ou le CSS. Utilisez les variables sémantiques.

✅ bg-primary-500 (Classe Tailwind)

✅ var(--primary-500) (Dans du CSS custom)

❌ background-color: #004080 (Interdit)

Conflits

Si un style PrimeNG résiste à une classe Tailwind, vérifiez l'ordre des imports dans styles.css. Tailwind doit pouvoir surcharger si besoin.
